steps:
  - task: cmdline@1.*
    inputs:
      filename: echo
      arguments: hello world {{this.[build.sourceVersion]}}! {{this.['build.sourceVersion']}}!
{{#each}}
- task: cmdline@1.*
    inputs:
      filename: echo
      arguments: hello world {{@key}}
{{/each}}
