steps:
  - task: cmdline@1.*
    inputs:
      filename: echo
      arguments: hello world {{@root.$['build.sourceVersion']}} or {{@root._yaml_preview}} or {{@root.$['_yaml_preview']}}???
# {{#each @root}}
# - task: cmdline@1.*
#     inputs:
#       filename: echo
#       arguments: hello world {{@key}}
# {{/each}}
