steps:
  - task: cmdline@1.*
    inputs:
      filename: echo
      arguments: hello world {{build\.sourceVersion}}! {{'build.sourceVersion'}}!
  - task: cmdline@1.*
    inputs:
      filename: echo
      arguments: hello world {{build\.sourceversion}}! {{'build.sourceversion'}}!
