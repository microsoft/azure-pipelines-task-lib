steps:
  - task: cmdline@1.*
    inputs:
      filename: echo
      arguments: hello world {{@root.$['build.sourceBranch']}} or {{@root.'build.sourceBranch'}} or {{'build.sourceBranch'}} or {{build.sourceBranch}}???
# {{#each @root}}
# - task: cmdline@1.*
#     inputs:
#       filename: echo
#       arguments: hello world {{@key}}
# {{/each}}
